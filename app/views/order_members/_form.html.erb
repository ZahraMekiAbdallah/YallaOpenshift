<%= form_for(@order_member) do |f| %>
  <% if @order_member.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order_member.errors.count, "error") %> prohibited this order_member from being saved:</h2>

      <ul>
      <% @order_member.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :order_id %><br>
    <%= f.number_field :order_id %>
  </div>
  <div class="field">
    <%= f.label :joined %><br>
    <%= f.text_field :joined %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
